package basic.data.type.range

fun main() {

    var grades = 0..100

    // To access and perform operations with a range, use the available properties and built-in methods
    // These are some examples:
    println(grades.count())
    println(grades.first)
    println(grades.last)
    println(grades.step)

}