package basic.data.type.character

fun main() {

    // Char can only hold one value (character)
    // Char value is surrounded by single quotes
    var alpha: Char = 'a'
    var numeric: Char = '1'

    println("Alpha character : " + alpha)
    println("Numeric character : " + numeric)

}